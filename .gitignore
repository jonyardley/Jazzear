# macOS
.DS_Store

# Rust
/target/
**/*.rs.bk

# Xcode (the project itself is xcodegen output — `just ios` regenerates it)
ios/build/
ios/DerivedData/
ios/*.xcodeproj/
*.xcodeproj/xcuserdata/
*.xcworkspace/xcuserdata/
xcuserdata/
*.ipa
*.dSYM.zip
*.dSYM

# Generated bindings — build output, never source (CLAUDE.md: bindings are a
# build precondition; regenerate, don't hand-edit or commit)
ios/generated/
crates/changes-ffi/generated/

# Environment
.env
.env.local

# Claude Code local settings
.claude/settings.local.json
