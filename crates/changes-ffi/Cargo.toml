[package]
name = "changes-ffi"
description = "UniFFI / typegen bridge crate — lands with M0-iOS"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
publish.workspace = true

[dependencies]
changes-core = { workspace = true }

[lints]
workspace = true
