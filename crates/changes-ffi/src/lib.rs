//! FFI bridge crate. The uniffi scaffolding + Swift typegen pipeline lands
//! with M0-iOS; until then this crate only anchors the workspace shape.

pub use changes_core::{Changes, Event, ViewModel};
